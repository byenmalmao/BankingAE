import LoginForm from './LoginForm.js';

function LoginPage() {
    return (
        <div className="flex justify-center items-center h-screen bg-gradient-to-r from-blue-50 to-gray-100">
            <LoginForm />
        </div>
    );
}

export default LoginPage;